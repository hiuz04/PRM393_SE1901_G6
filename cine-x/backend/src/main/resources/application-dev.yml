spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cinexdb}
    username: ${DB_USERNAME:cinex}
    password: ${DB_PASSWORD:cinex_local_2026}
    driver-class-name: org.postgresql.Driver
